import { createContext } from 'react';


   /* Object.keys(supplier).forEach((key) => {
        receiver[key] = supplier[key];
    })
    return receiver;*/


const enrolContext = createContext();

export default enrolContext; 